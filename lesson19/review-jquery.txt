--library--
-- local: tải về project từ trang chủ: http://jquery.com/
--api: (CDN, cloud,...):https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js
--- nhúng vào phần head; khuyến khích nên nhúng ở cuối trang html, trước thẻ đóng body

#syntax:
$(document).ready(function(){
    -- các câu lệnh sử dụng với jquery

    -- tương tác:
    $(selector).action(...){...}
       -- selector: html tag, #id, .class-name, css selector,....
       -- ví dụ:
           $('#zone1').css("color","red");
           $('#zone1').css({
               'color':'red'
               'background-color':'green'
           });
})